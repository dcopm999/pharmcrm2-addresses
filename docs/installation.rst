============
Installation
============

At the command line::

    $ easy_install pharmcrm2-addresses

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pharmcrm2-addresses
    $ pip install pharmcrm2-addresses
